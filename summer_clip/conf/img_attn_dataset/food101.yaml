defaults:
  - /prompting: tip_food101

dataset_name: food101

data:
  image_features_path: ${saved_paths.image_features.Food101_tip_test-RN50}

cache:
  image_features_path: ${saved_paths.image_features.Food101_tip_train-RN50}
  image_outs_path: ${saved_paths.logits.Food101_tip_train-RN50-tip}
