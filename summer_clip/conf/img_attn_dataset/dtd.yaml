defaults:
  - /prompting: tip_dtd

dataset_name: dtd

data:
  image_features_path: ${saved_paths.image_features.DescribableTextures_tip_test-RN50}

cache:
  image_features_path: ${saved_paths.image_features.DescribableTextures_tip_train-RN50}
  image_outs_path: ${saved_paths.logits.DescribableTextures_tip_train-RN50-tip}
